apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-boot-admin.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "spring-boot-admin.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
