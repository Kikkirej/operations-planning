apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres.fullname" . }}
  labels:
    app: {{ include "postgres.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "postgres.fullname" . }}
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
