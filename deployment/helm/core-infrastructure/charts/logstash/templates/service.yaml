{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "logstash.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "logstash.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: beats
{{- end }}
